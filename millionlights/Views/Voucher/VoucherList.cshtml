
@{
    ViewBag.Title = "VoucherList";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var voucherCodeList = ViewBag.VoucherCodeList;
}

<h2>VoucherList</h2>
<link href="~/Content/learn/css/color_scheme.css" rel="stylesheet" />
<script>
    $(document).ready(function () {
        $('#VoucherGridview').DataTable();
    });
</script>
<section id="main_content">
    <div class="container">
        <div class="row">
            <div class="col-xs-12 tableContent">
                <div class="table-header">
                    <b>Voucher Management</b>

                    @Html.ActionLink("Generate Voucher Code", "Create", "ProductCode", null, new { @class = "btnDataTable btnFocus" })

                </div>
                <div class="table-responsive">
                    <table id="VoucherGridview" class="table table-striped table-bordered table-hover fullWidth tablediv">
                        <thead>
                            <tr>
                                <th>Voucher Code</th>
                                <th>Voucher Type</th>
                                <th>Expiry Date</th>
                                <th>Course</th>
                                <th>Partner</th>
                                <th>Voucher Status</th>
                                <th>Activated By</th>
                                <th>Blocked On</th>
                                <th>Blocked Reason</th>
                                <th>Created On</th>
                                <th>Created By</th>
                                <th>Discount</th>
                                <th class="actionColWidth">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @*@foreach (var item in voucherCodeList)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.VouchCode)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.VoucherType)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ExpiryDate)
                        </td>

                        <td>
                            @Html.DisplayFor(modelItem => item.ActivatedCourse)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.PartnerName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.VoucherStatus)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ActivatedBy)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.BlockedOn)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.BlockedReason)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CreatedOn)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CreatedBy)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Discount)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Create", new { id = item.Id }, new { @class = "button_top editbtn" })
                                @Html.ActionLink("Delete", "Disabled", new { id = item.Id }, new { @class = "button_top deletbtn" })
                        </td>

                    </tr>
                }*@

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
</div>
</section>